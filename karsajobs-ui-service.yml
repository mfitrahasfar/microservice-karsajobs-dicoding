apiVersion: v1
kind: Service
metadata:
  name: karsajobs-ui
  labels:
    app: local
spec:
  type: NodePort
  selector:
    app: karsajobs-ui
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30000
